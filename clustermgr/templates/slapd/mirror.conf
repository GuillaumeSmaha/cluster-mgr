rid={r_id} provider={pprotocol}://{phost}:{pport} bindmethod=simple binddn="{replication_dn}" credentials={replication_pw} searchbase="o=gluu" schemachecking=on type=refreshAndPersist retry="60 +" logbase="cn=accesslog" logfilter="(&(objectClass=auditWriteObject)(reqResult=0))" syncdata=accesslog {pcert}
